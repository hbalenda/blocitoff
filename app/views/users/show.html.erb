<h1>Profile</h1>
  <p>Email: <%= @user.email %></p>
  <p>Getting things done since <%= @user.created_at.strftime("%b %d, %Y") %></p>
<div class="items">
  <ul>
  <%= render @user.items %>
  </ul>
</div>
<div class='new-item'>
<%= render 'items/form', item: Item.new, user: @user %>
</div>
