<tbody>
  <%= content_tag :tr, class: "info", id: "item-#{item.id}" do %>
    <td><%= link_to '', [current_user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></td>
    <td><%= item.name %></td>
    <td><%= time_left(item) + " left" %></td>
  <% end %>
</tbody>
